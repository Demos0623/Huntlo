# Huntlo relay uses only the Python standard library — no dependencies.
# This file exists so cloud Python runtimes (Render, Railway, …) detect the
# service and run `pip install -r requirements.txt` (which installs nothing).
